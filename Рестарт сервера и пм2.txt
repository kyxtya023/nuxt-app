Включён ли сервис:

systemctl status pm2-root


Если статус enabled и активен, значит всё ✅.

Чтобы убедиться, что автозапуск реально поднимет Nuxt после ребута — можешь проверить:

reboot


После перезагрузки зайди снова на сервер и глянь список процессов PM2:

pm2 list